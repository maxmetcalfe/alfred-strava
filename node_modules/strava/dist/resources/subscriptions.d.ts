import { Request } from '../request';
declare type Subscription = {
    id: number;
};
declare type SubscriptionCreationRequest = {
    callback_url: string;
    verify_token: string;
};
declare type SubscriptionCreationResponse = {
    id: number;
};
export declare class Subscriptions {
    private readonly request;
    constructor(request: Request);
    createSubscription(params: SubscriptionCreationRequest): Promise<SubscriptionCreationResponse>;
    deleteSubscription(id: number): Promise<any>;
    getSubscriptions(): Promise<Subscription[]>;
}
export {};
